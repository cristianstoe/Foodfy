{% extends "layout.njk" %}

{% block content %}
        <div class="containerRecipe">
            {% for item in items%}
                <div class="card" id="{{loop.index0}}">
                    <div class="cardImg">
                        <img src="{{item.recipe_url}}" alt="">
                    </div>
                    <div class="cardTitle">
                        <h3>{{item.name}}</h3>
                    </div>
                    <div class="cardAuthor">
                        <p>por
                            {{item.author}}</p>
                    </div>
                </div>
                {%endfor%}
            </div>

        </div>
        <div class="footer-box">
        </div>
            <script src="scripts.js"></script>

{% endblock %}
